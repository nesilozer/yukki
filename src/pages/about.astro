npm---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="About Us - Yukki's Corner"
  description="Learn about Yukki, our adorable Shiba Inu puppy, and our story together"
>
  <div class="section">
    <div class="container">
      <div class="about-content card">
        <h1>About Yukki</h1>

        <div class="image-container">
          <img
            src="/yukki-relaxing-in-bed.webp"
            alt="Yukki relaxing comfortably in bed, looking peaceful and content"
            width="1200"
            height="1600"
            loading="lazy"
            decoding="async"
          />
        </div>

        <div class="story-section">
          <h2>Our Story</h2>
          <div class="story-content">
            <p class="story-placeholder">
              This is where you can add Yukki's story! Tell us about:
            </p>
            <ul class="story-ideas">
              <li>How Yukki came into your life</li>
              <li>What makes Yukki special</li>
              <li>Favorite activities and quirks</li>
              <li>Memorable moments together</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </div>

  <style>
    .about-content {
      max-width: 800px;
      margin: 0 auto;
    }

    h1 {
      text-align: center;
      color: var(--color-primary);
      margin-bottom: var(--space-xl);
    }

    .image-container {
      width: 100%;
      max-width: 600px;
      margin: 0 auto var(--space-2xl);
      border-radius: var(--radius-lg);
      overflow: hidden;
      box-shadow: var(--shadow-lg);
      transition: transform var(--transition-base);
    }

    .image-container:hover {
      transform: scale(1.02);
    }

    .image-container img {
      width: 100%;
      height: auto;
      display: block;
      object-fit: cover;
    }

    .story-section h2 {
      color: var(--color-accent);
      margin-bottom: var(--space-lg);
      padding-bottom: var(--space-sm);
      border-bottom: 3px solid var(--color-primary-light);
    }

    .story-content {
      line-height: 1.8;
    }

    .story-placeholder {
      padding: var(--space-lg);
      background: linear-gradient(135deg, var(--color-secondary-light) 0%, var(--color-secondary) 100%);
      border-left: 4px solid var(--color-primary);
      border-radius: var(--radius-md);
      color: var(--color-text-light);
      font-style: italic;
      margin-bottom: var(--space-md);
    }

    .story-ideas {
      list-style: none;
      padding-left: var(--space-lg);
    }

    .story-ideas li {
      position: relative;
      padding: var(--space-sm) 0;
      color: var(--color-text-light);
    }

    .story-ideas li::before {
      content: '🐾';
      position: absolute;
      left: calc(var(--space-lg) * -1);
    }

    @media (max-width: 768px) {
      .image-container {
        margin-bottom: var(--space-xl);
      }

      .story-ideas {
        padding-left: var(--space-md);
      }

      .story-ideas li::before {
        left: calc(var(--space-md) * -1);
      }
    }
  </style>
</Layout>
