---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="Blog - Life with Yukki"
  description="Read stories and updates about Yukki the Shiba Inu puppy"
>
  <div class="section">
    <div class="container">
      <div class="page-header">
        <h1>Yukki's Blog</h1>
        <p class="page-subtitle">Adventures, memories, and moments worth sharing</p>
      </div>

      <div class="stories-container">
        <!-- Welcome Home Yukki -->
        <article class="story-card card">
          <div class="story-header">
            <h2>Welcome Home Yukki</h2>
            <time class="story-date" datetime="2026-01-14">January 14, 2026</time>
          </div>
          <div class="story-image">
            <img
              src="/shiba-inu-puppies-playing.webp"
              alt="Four adorable Shiba Inu puppies at Kolgerhouse Kennel"
              loading="lazy"
              decoding="async"
            />
          </div>
          <div class="story-content">
            <p>
              My girlfriend always wanted a Shiba Inu. Not just "wanted" in a casual way.
              This was her lifelong dream. The kind of dream that doesn't fade with time.
              Here is our story of how we found and brought home Yukki from
              Kolgerhouse Kennel in Belarus.
            </p>
            <a href="/blog/welcome-home-yukki" class="read-more">Read full story →</a>
          </div>
        </article>
      </div>
    </div>
  </div>

  <style>
    .page-header {
      text-align: center;
      margin-bottom: var(--space-3xl);
    }

    .page-header h1 {
      color: var(--color-primary);
      font-size: var(--font-size-4xl);
      margin-bottom: var(--space-md);
    }

    .page-subtitle {
      font-size: var(--font-size-xl);
      color: var(--color-text-light);
    }

    .stories-container {
      max-width: 900px;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      gap: var(--space-2xl);
    }

    .story-card {
      transition: transform var(--transition-base);
    }

    .story-card:hover {
      transform: translateY(-4px);
    }

    .story-header {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      margin-bottom: var(--space-lg);
      padding-bottom: var(--space-md);
      border-bottom: 2px solid var(--color-secondary);
    }

    .story-header h2 {
      color: var(--color-primary);
      font-size: var(--font-size-2xl);
      margin: 0;
    }

    .story-date {
      color: var(--color-text-light);
      font-size: var(--font-size-sm);
      font-style: italic;
    }

    .story-image {
      width: 100%;
      max-width: 600px;
      margin: 0 auto var(--space-xl);
      border-radius: var(--radius-lg);
      overflow: hidden;
      box-shadow: var(--shadow-md);
    }

    .story-image img {
      width: 100%;
      height: auto;
      display: block;
    }

    .story-content {
      line-height: 1.8;
    }

    .story-content p {
      color: var(--color-text);
      margin-bottom: var(--space-lg);
      font-size: var(--font-size-lg);
    }

    .read-more {
      color: var(--color-primary);
      font-weight: 600;
      transition: all var(--transition-fast);
      display: inline-flex;
      align-items: center;
      gap: var(--space-xs);
    }

    .read-more:hover {
      color: var(--color-primary-dark);
      text-decoration: underline;
    }

    @media (max-width: 768px) {
      .story-header {
        flex-direction: column;
        align-items: flex-start;
        gap: var(--space-sm);
      }

      .story-content p {
        font-size: var(--font-size-base);
      }
    }
  </style>
</Layout>
