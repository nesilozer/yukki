---
import Layout from '../../../layouts/Layout.astro';

const chartSchema = {
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "Таблица роста щенка сиба-ину",
  "description": "Интерактивная таблица роста для отслеживания веса щенка сиба-ину и сравнения со средними показателями породы.",
  "applicationCategory": "UtilitiesApplication",
  "operatingSystem": "Any",
  "offers": {
    "@type": "Offer",
    "price": "0",
    "priceCurrency": "USD"
  }
};
---

<Layout
  title="Таблица роста щенка сиба-ину | Жизнь с Юкки"
  description="Бесплатная таблица роста щенка сиба-ину. Отслеживайте вес по возрасту и сравнивайте со средними показателями. Узнайте ожидаемый вес в 8 недель, 3 месяца, 6 месяцев и взрослого."
  lang="ru"
>
  <script slot="head" type="application/ld+json" set:html={JSON.stringify(chartSchema)} />

  <div class="section">
    <div class="container">
      <div class="tool-container">
        <!-- Header -->
        <div class="tool-header">
          <nav class="breadcrumb">
            <a href="/ru">Главная</a>
            <span class="separator">&rarr;</span>
            <a href="/ru/tools">Инструменты</a>
            <span class="separator">&rarr;</span>
            <span>Таблица роста</span>
          </nav>
          <h1>Таблица роста щенка сиба-ину</h1>
          <p class="tool-intro">Отслеживайте вес вашего сиба-ину и сравнивайте со средними показателями породы. Сиба-ину обычно достигают взрослого веса в 12-18 месяцев.</p>
        </div>

        <!-- Weight Checker -->
        <div class="weight-checker card">
          <h2>Проверьте вес вашего щенка</h2>
          <div class="checker-form">
            <div class="form-group">
              <label for="puppyAge">Возраст щенка</label>
              <select id="puppyAge">
                <option value="">Выберите возраст...</option>
                <option value="8">8 недель (2 месяца)</option>
                <option value="12">12 недель (3 месяца)</option>
                <option value="16">16 недель (4 месяца)</option>
                <option value="20">20 недель (5 месяцев)</option>
                <option value="24">24 недели (6 месяцев)</option>
                <option value="32">32 недели (8 месяцев)</option>
                <option value="40">40 недель (10 месяцев)</option>
                <option value="52">52 недели (12 месяцев)</option>
                <option value="78">18 месяцев (Взрослый)</option>
              </select>
            </div>
            <div class="form-group">
              <label for="puppyWeight">Текущий вес</label>
              <div class="weight-input-group">
                <input type="number" id="puppyWeight" placeholder="Введите вес" step="0.1" min="0" max="30">
                <select id="weightUnit">
                  <option value="kg">кг</option>
                  <option value="lbs">фунты</option>
                </select>
              </div>
            </div>
            <div class="form-group">
              <label for="puppyGender">Пол</label>
              <select id="puppyGender">
                <option value="male">Мальчик</option>
                <option value="female">Девочка</option>
              </select>
            </div>
            <button class="btn" id="checkWeightBtn">Проверить</button>
          </div>
          <div class="result-container" id="resultContainer" style="display: none;">
          </div>
        </div>

        <!-- Growth Chart Table -->
        <div class="chart-section card">
          <h2>Вес сиба-ину по возрасту</h2>
          <p class="chart-note">Средние диапазоны веса для здоровых сиба-ину. Индивидуальные показатели могут варьироваться в зависимости от генетики, питания и активности.</p>

          <div class="table-wrapper">
            <table class="growth-table">
              <thead>
                <tr>
                  <th>Возраст</th>
                  <th>Вес мальчика</th>
                  <th>Вес девочки</th>
                  <th>Этапы развития</th>
                </tr>
              </thead>
              <tbody>
                <tr>
                  <td><strong>8 недель</strong></td>
                  <td>2.0 - 3.0 кг</td>
                  <td>1.8 - 2.7 кг</td>
                  <td>Готов к новому дому, начало социализации</td>
                </tr>
                <tr>
                  <td><strong>12 недель</strong></td>
                  <td>3.0 - 4.5 кг</td>
                  <td>2.7 - 4.0 кг</td>
                  <td>Продолжение вакцинации, активный рост</td>
                </tr>
                <tr>
                  <td><strong>16 недель</strong></td>
                  <td>4.5 - 6.0 кг</td>
                  <td>4.0 - 5.5 кг</td>
                  <td>Можно начинать дрессировку, смена зубов</td>
                </tr>
                <tr>
                  <td><strong>20 недель</strong></td>
                  <td>5.5 - 7.0 кг</td>
                  <td>5.0 - 6.5 кг</td>
                  <td>Подростковый период, проверка границ</td>
                </tr>
                <tr>
                  <td><strong>6 месяцев</strong></td>
                  <td>6.5 - 8.0 кг</td>
                  <td>5.5 - 7.0 кг</td>
                  <td>~70% взрослого веса, рост замедляется</td>
                </tr>
                <tr>
                  <td><strong>8 месяцев</strong></td>
                  <td>7.5 - 9.5 кг</td>
                  <td>6.5 - 8.0 кг</td>
                  <td>~85% взрослого веса</td>
                </tr>
                <tr>
                  <td><strong>10 месяцев</strong></td>
                  <td>8.5 - 10.5 кг</td>
                  <td>7.0 - 9.0 кг</td>
                  <td>~95% взрослого веса</td>
                </tr>
                <tr>
                  <td><strong>12 месяцев</strong></td>
                  <td>9.0 - 11.0 кг</td>
                  <td>7.5 - 9.5 кг</td>
                  <td>Почти взрослый вес, продолжает взрослеть</td>
                </tr>
                <tr class="adult-row">
                  <td><strong>Взрослый (18+ мес)</strong></td>
                  <td>10 - 12 кг</td>
                  <td>8 - 10 кг</td>
                  <td>Полностью вырос, поддержание здорового веса</td>
                </tr>
              </tbody>
            </table>
          </div>
        </div>

        <!-- Growth Tips -->
        <div class="tips-section card">
          <h2>Советы для здорового роста</h2>
          <div class="tips-grid">
            <div class="tip">
              <div class="tip-icon">🍖</div>
              <h3>Правильное питание</h3>
              <p>Кормите качественным кормом для щенков, соответствующим возрасту. Избегайте перекармливания - сиба-ину легко набирают вес.</p>
            </div>
            <div class="tip">
              <div class="tip-icon">🏃</div>
              <h3>Регулярные прогулки</h3>
              <p>Щенкам нужны умеренные нагрузки. Избегайте чрезмерных упражнений для маленьких щенков - это может повредить суставы.</p>
            </div>
            <div class="tip">
              <div class="tip-icon">⚕️</div>
              <h3>Визиты к ветеринару</h3>
              <p>Регулярные осмотры обеспечивают здоровое развитие. Ветеринар отследит вес и выявит проблемы на ранней стадии.</p>
            </div>
            <div class="tip">
              <div class="tip-icon">📏</div>
              <h3>Отслеживание прогресса</h3>
              <p>Взвешивайте щенка еженедельно первые 6 месяцев, затем ежемесячно. Резкие изменения могут указывать на проблемы со здоровьем.</p>
            </div>
          </div>
        </div>

        <!-- FAQ Section -->
        <div class="faq-section card">
          <h2>Частые вопросы</h2>
          <div class="faq-list">
            <details class="faq-item">
              <summary>Когда сиба-ину перестают расти?</summary>
              <p>Сиба-ину обычно достигают полного роста к 12 месяцам, но продолжают набирать массу до 18 месяцев. Мальчики могут взрослеть немного дольше девочек.</p>
            </details>
            <details class="faq-item">
              <summary>Мой щенок весит меньше нормы. Стоит ли беспокоиться?</summary>
              <p>Небольшие отклонения нормальны. Если щенок весит на 15% меньше среднего, проконсультируйтесь с ветеринаром. Генетика, активность и питание влияют на вес.</p>
            </details>
            <details class="faq-item">
              <summary>Мой щенок кажется толстым. Что делать?</summary>
              <p>Сиба-ину склонны к набору веса. Уменьшите количество лакомств, контролируйте порции и увеличьте активность. При необходимости обратитесь к ветеринару за диетой.</p>
            </details>
            <details class="faq-item">
              <summary>Как часто нужно взвешивать щенка сиба-ину?</summary>
              <p>Взвешивайте еженедельно первые 6 месяцев, затем ежемесячно до полного взросления. После этого ежемесячные или двухмесячные проверки помогут поддерживать здоровый вес.</p>
            </details>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script>
    const growthData = {
      male: {
        8: { min: 2.0, max: 3.0 },
        12: { min: 3.0, max: 4.5 },
        16: { min: 4.5, max: 6.0 },
        20: { min: 5.5, max: 7.0 },
        24: { min: 6.5, max: 8.0 },
        32: { min: 7.5, max: 9.5 },
        40: { min: 8.5, max: 10.5 },
        52: { min: 9.0, max: 11.0 },
        78: { min: 10.0, max: 12.0 }
      },
      female: {
        8: { min: 1.8, max: 2.7 },
        12: { min: 2.7, max: 4.0 },
        16: { min: 4.0, max: 5.5 },
        20: { min: 5.0, max: 6.5 },
        24: { min: 5.5, max: 7.0 },
        32: { min: 6.5, max: 8.0 },
        40: { min: 7.0, max: 9.0 },
        52: { min: 7.5, max: 9.5 },
        78: { min: 8.0, max: 10.0 }
      }
    };

    function checkWeight() {
      const ageSelect = document.getElementById('puppyAge') as HTMLSelectElement;
      const weightInput = document.getElementById('puppyWeight') as HTMLInputElement;
      const unitSelect = document.getElementById('weightUnit') as HTMLSelectElement;
      const genderSelect = document.getElementById('puppyGender') as HTMLSelectElement;
      const resultContainer = document.getElementById('resultContainer');

      const age = parseInt(ageSelect.value);
      let weight = parseFloat(weightInput.value);
      const unit = unitSelect.value;
      const gender = genderSelect.value as 'male' | 'female';

      if (!age || !weight) {
        if (resultContainer) {
          resultContainer.style.display = 'block';
          resultContainer.innerHTML = `
            <div class="result result-error">
              <p>Пожалуйста, укажите возраст и вес для проверки.</p>
            </div>
          `;
        }
        return;
      }

      if (unit === 'lbs') {
        weight = weight * 0.453592;
      }

      const range = growthData[gender][age];
      const midpoint = (range.min + range.max) / 2;

      let status, statusClass, message, emoji;

      if (weight < range.min * 0.85) {
        status = 'Недостаточный вес';
        statusClass = 'result-warning';
        emoji = '⚠️';
        message = `Ваш щенок весит меньше ожидаемого диапазона. Рекомендуем проконсультироваться с ветеринаром, чтобы исключить проблемы со здоровьем и обсудить питание.`;
      } else if (weight < range.min) {
        status = 'Немного ниже среднего';
        statusClass = 'result-info';
        emoji = '📊';
        message = `Ваш щенок немного ниже типичного диапазона, но это может быть нормой. Следите за ростом и обеспечьте правильное питание.`;
      } else if (weight <= range.max) {
        status = 'Здоровый вес';
        statusClass = 'result-success';
        emoji = '✅';
        message = `Отличные новости! Вес вашего щенка находится в здоровом диапазоне для его возраста и пола.`;
      } else if (weight <= range.max * 1.15) {
        status = 'Немного выше среднего';
        statusClass = 'result-info';
        emoji = '📊';
        message = `Ваш щенок немного выше типичного диапазона. Это может быть нормально, но следите за чрезмерным набором веса.`;
      } else {
        status = 'Избыточный вес';
        statusClass = 'result-warning';
        emoji = '⚠️';
        message = `Ваш щенок весит больше ожидаемого диапазона. Рекомендуем уменьшить количество лакомств и порций, а также проконсультироваться с ветеринаром.`;
      }

      const displayMin = unit === 'lbs' ? (range.min * 2.20462).toFixed(1) : range.min.toFixed(1);
      const displayMax = unit === 'lbs' ? (range.max * 2.20462).toFixed(1) : range.max.toFixed(1);
      const displayWeight = unit === 'lbs' ? (weight * 2.20462).toFixed(1) : weight.toFixed(1);
      const unitLabel = unit === 'lbs' ? 'фунтов' : 'кг';

      if (resultContainer) {
        resultContainer.style.display = 'block';
        resultContainer.innerHTML = `
          <div class="result ${statusClass}">
            <div class="result-header">
              <span class="result-emoji">${emoji}</span>
              <span class="result-status">${status}</span>
            </div>
            <p class="result-message">${message}</p>
            <div class="result-details">
              <div class="detail">
                <span class="detail-label">Ваш щенок</span>
                <span class="detail-value">${displayWeight} ${unitLabel}</span>
              </div>
              <div class="detail">
                <span class="detail-label">Ожидаемый диапазон</span>
                <span class="detail-value">${displayMin} - ${displayMax} ${unitLabel}</span>
              </div>
            </div>
          </div>
        `;
      }
    }

    document.getElementById('checkWeightBtn')?.addEventListener('click', checkWeight);

    document.getElementById('puppyWeight')?.addEventListener('keypress', (e) => {
      if (e.key === 'Enter') {
        checkWeight();
      }
    });
  </script>

  <style>
    .tool-container {
      max-width: 900px;
      margin: 0 auto;
    }

    .breadcrumb {
      display: flex;
      align-items: center;
      gap: var(--space-sm);
      margin-bottom: var(--space-lg);
      font-size: var(--font-size-sm);
      color: var(--color-text-light);
    }

    .breadcrumb a {
      color: var(--color-primary);
      transition: color var(--transition-fast);
    }

    .breadcrumb a:hover {
      color: var(--color-primary-dark);
    }

    .tool-header {
      text-align: center;
      margin-bottom: var(--space-2xl);
    }

    .tool-header h1 {
      color: var(--color-primary);
      font-size: var(--font-size-3xl);
      margin-bottom: var(--space-md);
    }

    .tool-intro {
      color: var(--color-text-light);
      font-size: var(--font-size-lg);
      line-height: 1.6;
      max-width: 600px;
      margin: 0 auto;
    }

    .weight-checker {
      margin-bottom: var(--space-2xl);
    }

    .weight-checker h2 {
      color: var(--color-accent);
      margin-bottom: var(--space-xl);
      text-align: center;
    }

    .checker-form {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: var(--space-lg);
      align-items: end;
    }

    .form-group {
      display: flex;
      flex-direction: column;
      gap: var(--space-sm);
    }

    .form-group label {
      font-weight: 600;
      color: var(--color-text);
      font-size: var(--font-size-sm);
    }

    .form-group select,
    .form-group input {
      padding: var(--space-md);
      border: 2px solid var(--color-secondary);
      border-radius: var(--radius-md);
      font-size: var(--font-size-base);
      background-color: var(--color-white);
      transition: border-color var(--transition-fast);
    }

    .form-group select:focus,
    .form-group input:focus {
      outline: none;
      border-color: var(--color-primary);
    }

    .weight-input-group {
      display: flex;
      gap: var(--space-sm);
    }

    .weight-input-group input {
      flex: 1;
    }

    .weight-input-group select {
      width: 90px;
    }

    .result-container {
      margin-top: var(--space-xl);
    }

    .result {
      padding: var(--space-xl);
      border-radius: var(--radius-md);
      text-align: center;
    }

    .result-success {
      background-color: #dcfce7;
      border: 2px solid #22c55e;
    }

    .result-warning {
      background-color: #fef3c7;
      border: 2px solid #f59e0b;
    }

    .result-info {
      background-color: #e0f2fe;
      border: 2px solid #0ea5e9;
    }

    .result-error {
      background-color: #fee2e2;
      border: 2px solid #ef4444;
    }

    .result-header {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: var(--space-sm);
      margin-bottom: var(--space-md);
    }

    .result-emoji {
      font-size: 2rem;
    }

    .result-status {
      font-size: var(--font-size-xl);
      font-weight: 700;
      color: var(--color-text);
    }

    .result-message {
      color: var(--color-text);
      margin-bottom: var(--space-lg);
      line-height: 1.6;
    }

    .result-details {
      display: flex;
      justify-content: center;
      gap: var(--space-2xl);
      flex-wrap: wrap;
    }

    .detail {
      display: flex;
      flex-direction: column;
      gap: var(--space-xs);
    }

    .detail-label {
      font-size: var(--font-size-sm);
      color: var(--color-text-light);
    }

    .detail-value {
      font-size: var(--font-size-lg);
      font-weight: 600;
      color: var(--color-text);
    }

    .chart-section {
      margin-bottom: var(--space-2xl);
    }

    .chart-section h2 {
      color: var(--color-accent);
      margin-bottom: var(--space-md);
    }

    .chart-note {
      color: var(--color-text-light);
      font-size: var(--font-size-sm);
      margin-bottom: var(--space-xl);
    }

    .table-wrapper {
      overflow-x: auto;
    }

    .growth-table {
      width: 100%;
      border-collapse: collapse;
      font-size: var(--font-size-sm);
    }

    .growth-table th,
    .growth-table td {
      padding: var(--space-md);
      text-align: left;
      border-bottom: 1px solid var(--color-secondary);
    }

    .growth-table th {
      background-color: var(--color-primary);
      color: var(--color-white);
      font-weight: 600;
    }

    .growth-table th:first-child {
      border-radius: var(--radius-md) 0 0 0;
    }

    .growth-table th:last-child {
      border-radius: 0 var(--radius-md) 0 0;
    }

    .growth-table tr:hover {
      background-color: var(--color-secondary-light);
    }

    .growth-table .adult-row {
      background-color: var(--color-secondary);
      font-weight: 500;
    }

    .tips-section {
      margin-bottom: var(--space-2xl);
    }

    .tips-section h2 {
      color: var(--color-accent);
      margin-bottom: var(--space-xl);
    }

    .tips-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: var(--space-lg);
    }

    .tip {
      text-align: center;
      padding: var(--space-lg);
      background-color: var(--color-secondary-light);
      border-radius: var(--radius-md);
    }

    .tip-icon {
      font-size: 2.5rem;
      margin-bottom: var(--space-md);
    }

    .tip h3 {
      color: var(--color-primary);
      font-size: var(--font-size-base);
      margin-bottom: var(--space-sm);
    }

    .tip p {
      color: var(--color-text-light);
      font-size: var(--font-size-sm);
      line-height: 1.6;
    }

    .faq-section h2 {
      color: var(--color-accent);
      margin-bottom: var(--space-xl);
    }

    .faq-list {
      display: flex;
      flex-direction: column;
      gap: var(--space-md);
    }

    .faq-item {
      border: 1px solid var(--color-secondary);
      border-radius: var(--radius-md);
      overflow: hidden;
    }

    .faq-item summary {
      padding: var(--space-lg);
      cursor: pointer;
      font-weight: 600;
      color: var(--color-text);
      background-color: var(--color-secondary-light);
      transition: background-color var(--transition-fast);
      list-style: none;
    }

    .faq-item summary::-webkit-details-marker {
      display: none;
    }

    .faq-item summary::before {
      content: '+ ';
      color: var(--color-primary);
    }

    .faq-item[open] summary::before {
      content: '- ';
    }

    .faq-item summary:hover {
      background-color: var(--color-secondary);
    }

    .faq-item p {
      padding: var(--space-lg);
      color: var(--color-text-light);
      line-height: 1.7;
      margin: 0;
    }

    @media (max-width: 768px) {
      .tool-header h1 {
        font-size: var(--font-size-2xl);
      }

      .checker-form {
        grid-template-columns: 1fr;
      }

      .growth-table {
        font-size: var(--font-size-xs);
      }

      .growth-table th,
      .growth-table td {
        padding: var(--space-sm);
      }

      .result-details {
        flex-direction: column;
        gap: var(--space-md);
      }
    }
  </style>
</Layout>
