---
import Layout from '../../layouts/Layout.astro';
---

<Layout
  title="Страница не найдена - Жизнь с Юкки"
  description="Упс! Юкки не смог найти эту страницу."
  lang="ru"
>
  <section class="error-page">
    <div class="container">
      <div class="error-content">
        <div class="error-image">
          <img
            src="/yukki-404.webp"
            alt="Юкки сиба-ину лежит на спине с растерянным видом"
            width="600"
            height="450"
            loading="eager"
          />
        </div>
        <div class="error-text">
          <h1>404</h1>
          <p class="error-title">Упс! Страница не найдена</p>
          <p class="error-message">
            Юкки всё обнюхал, но не смог найти эту страницу.
          </p>
          <div class="error-buttons">
            <a href="/ru" class="btn">На главную</a>
            <a href="/ru/blog" class="btn btn-secondary">Читать блог</a>
          </div>
        </div>
      </div>
    </div>
  </section>

  <style>
    .error-page {
      min-height: 70vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: var(--space-3xl) var(--space-md);
      background: linear-gradient(135deg, var(--color-secondary-light) 0%, var(--color-secondary) 100%);
    }

    .container {
      width: 100%;
      display: flex;
      justify-content: center;
    }

    .error-content {
      text-align: center;
      max-width: 600px;
    }

    .error-image {
      margin-bottom: var(--space-2xl);
    }

    .error-image img {
      width: 100%;
      max-width: 500px;
      height: auto;
      border-radius: var(--radius-xl);
      box-shadow: var(--shadow-xl);
      transform: rotate(-2deg);
      transition: transform var(--transition-base);
    }

    .error-image img:hover {
      transform: rotate(0deg) scale(1.02);
    }

    .error-text h1 {
      font-size: 5rem;
      color: var(--color-primary);
      line-height: 1;
      margin-bottom: var(--space-sm);
      text-shadow: 4px 4px 0 var(--color-primary-light);
    }

    .error-title {
      font-size: var(--font-size-2xl);
      color: var(--color-accent);
      font-weight: 700;
      margin-bottom: var(--space-md);
    }

    .error-message {
      font-size: var(--font-size-lg);
      color: var(--color-text-light);
      margin-bottom: var(--space-xl);
      line-height: 1.6;
    }

    .error-buttons {
      display: flex;
      gap: var(--space-md);
      justify-content: center;
      flex-wrap: wrap;
    }

    @media (max-width: 768px) {
      .error-text h1 {
        font-size: 4rem;
      }

      .error-title {
        font-size: var(--font-size-xl);
      }

      .error-message {
        font-size: var(--font-size-base);
      }

      .error-buttons {
        flex-direction: column;
        align-items: center;
      }

      .error-buttons .btn {
        width: 100%;
        max-width: 250px;
      }
    }
  </style>
</Layout>
